# 学習計画（2026/9 〜 2027 春）

## 判断の優先順位

迷ったらこれだけ見る。

1. タスクアプリを前に進める
2. 詰まったWeb技術を学ぶ
3. 出てきたセキュリティをCTF/徳丸本で学ぶ
4. 学んだことをアプリに戻して試す

競プロはこれと独立。毎日30分〜1時間。

**フレームワークは React に確定。**（スタンプラリーアプリがNext.js＝Reactベースなので、Reactを学べばあれが読めるようになる）

---

## 週の配分

- 競プロ：毎日1問（週6〜8）＋ **週1日は新規なし・復習のみ**
- Web：週4〜5ブロック（1回2〜3時間）を、開発 / CTF / 徳丸本 に配分

1日は「競プロ＋もう1つ」まで。曜日固定ではなく週単位で数える。

---

## AIの使い方（Phase 1〜3）

**家庭教師として使う。代筆者として使わない。**

- OK：エラーの意味を聞く／自分のコードを貼って原因を聞く／方針だけ聞く／文法の確認
- NG：丸投げして出てきたものをそのまま使う

ルール：先に15分は自分で考える。答えをもらったらコピペせず手で打ち直す。「なぜ動いたか」を自分の言葉で言えるか確認する。

Phase 2以降は緩めてよい。ただし **Phase 3の認証まわりは再び厳しく**（応募課題の中身そのものなので手で書く）。

---

## Phase 0：〜9月上旬

**目標：JSを終わらせる**

- [ ] MDN Core「JavaScriptによる動的スクリプティング」全34項目
  - 29 ネットワークリクエスト・30 JSON は読むだけにせず必ず書く
  - 33 デバッグとエラー処理も飛ばさない
- [ ] MDN Extensions「非同期JavaScript」1〜3（入門 / プロミスの使い方 / プロミスベースAPIの実装）
  - 4 ワーカー入門は優先度低・流す程度でよい
- [ ] MDN Extensions「クライアントサイドWeb API」→ **「クライアント側ストレージ」1ページのみ**（他4ページは不要）
- [ ] MDN Core「バージョン管理」→ **今すぐ並行で開始**。GitHubに `task-app` リポジトリを用意
- [ ] PortSwigger 週1（Essential skills、Burp操作に慣れる）
- [ ] 徳丸本：HTTP / Cookie / 同一オリジン のみ

**終了条件**：fetchでAPIを叩ける・JSONを扱える・async/awaitを説明できる・Gitを普通に使える

---

## Phase 1：9月（素のTypeScript + Todo）

**目標：ブラウザだけで動くTS製Todo。フレームワークは使わない。**

> ここを飛ばさない。「データとDOMを手で同期させるのがしんどい」を体験しておかないと、Reactが何を解決しているのか分からないまま使うことになる。

- W1：`Task` 型を定義してDOM操作開始
  - TSは事前学習不要。書きながら覚える。genericsやユーティリティ型は当面不要
  - 不安なら公式の "TypeScript for JavaScript Programmers" を半日だけ／または最初はJSで書いて `.ts` にリネームして型を足す
  - `strict` は最初オフでもよいが、最終的にはオンにする
- W2：追加 / 表示 / 削除 / 完了切替 / 件数
- W3：localStorage（`JSON.stringify` ⇄ `JSON.parse`）
  - `getItem` は未保存時 `null` → そのまま `parse` するとエラー
  - `Date` は復元されない → `createdAt` は `number`（`Date.now()`）で持つのが簡単
- W4：ファイル分割・エラー処理・README → GitHub公開

**終了条件**：公開済み／自力で書いた（丸投げ生成なし）／自分でコードを説明できる／**素のDOM操作で何がしんどかったかを言語化できる**

---

## Phase 1.5：10月頃（React）

- [ ] MDN Core「JavaScriptフレームワークとライブラリー」全9ページ
  - 1〜2（概要・フレームワークの機能）→ 3〜9 がReactチュートリアル
  - 題材がToDoなので、**自分のタスクアプリをReactに移植しながら**読む
- [ ] 終わったら **スタンプラリーアプリのコードを読み返す**
  - まず読めるようになってから読む。全面的な作り直しはしない
  - 気になる部分から少しずつ直す

---

## Phase 2：10〜11月（API化）

**目標：localStorage → Express + DB**

- Extensions「サーバーサイド → 第一歩」の「ウェブサイトのセキュリティ」を先に読む
- 10月前半：HTTP / メソッド / ステータスコード / REST
- 10月後半：Todo API（GET・POST・PATCH・DELETE `/api/tasks`）
- 11月：SQLite導入（`User` / `Task`）
- Phase末：**IDOR を検証**（他人のtaskが取れないか）

※ DBは想定の1.5倍かかる前提でバッファを取る

---

## Phase 3：11月後半〜1月（認証・認可）

- 登録：パスワードを平文保存しない
- ログイン：Cookie → セッション
- 認可：User A が User B のtaskを取れないようにする
- Phase末：**CSRF・セッション（Secure / HttpOnly / SameSite / fixation）を検証**

認証（Authentication）と認可（Authorization）の違いを説明できるように。

**ここはAIに丸投げしない。手で書く。**

---

## Phase 4：随時（脆弱版で攻撃 → 修正）

各Phaseの末尾に分散。XSS / SQLi / CSRF / IDOR / セッション。

`vulnerable` ブランチで再現 → 攻撃 → 原因を理解 → `secure` で修正 → `vulnerabilities.md` に記録。

> **脆弱版はローカル限定（localhostバインド）。絶対にデプロイしない。**

記録の型：脆弱だった実装 / 攻撃方法 / なぜ通ったか / 修正 / 修正後の確認

※ Phase 3後、**スタンプラリーアプリも自分のアプリとして点検**（特にIDOR系）。公開しているなら価値が高い。

---

## 競プロ

**9〜11月**：個人精進。全探索・二分探索・累積和・DFS/BFS・DP・グラフ・貪欲・Union-Find

記録は問題数だけにしない → 自力AC / 解説AC / 復習 の内訳。解説ACは後日必ず再演。

**12月〜春**：ICPC過去問 + チーム練を追加。月2〜4回 → 週1 → 週2 と増やす。

> **チーム3人の確保は最優先。今すぐ動く。**

---

## MDNの使い方

**モジュール単位ではなくページ単位で必要なものを取る。** 全部読むための教材ではない。

- 学習エリア（Learn）＝通読向け
- リファレンス（Web/JavaScript/Reference）＝辞書。通読しない

---

## 成果物

```
task-app/
├── frontend/  backend/  database/
├── README.md
└── docs/
    ├── architecture.md
    ├── security.md
    ├── vulnerabilities.md   ← 最重要
    └── learning-log.md
```

---

## 週次チェック（日曜10分）

```
## Week XX

### 競プロ
- [ ] 6〜8問
- [ ] 解説ACを復習（復習日を消化したか）

### Web（4〜5ブロック）
- [ ] 内訳：開発 __ / CTF __ / 徳丸本 __
- [ ] commitした

### タスクアプリ
今週の実装：
来週やること：

### 学習記録
理解したこと：
まだ分からないこと：

### 来週の最重要タスク（1個だけ）
→
```

---

## 月末チェック（30分）

| 時期 | 到達点 |
|---|---|
| 9月末 | JS完了 / 素のTSでTodo完成・公開 |
| 10月末 | React習得 / スタンプラリー読み返し / Express・REST API |
| 11月末 | DB導入 / ユーザー着手 |
| 12月末 | ログイン・セッション・認可 |
| 1月末 | 脆弱版で攻撃・修正・記録 |
| 2〜3月 | セキュキャン応募準備 / ICPCチーム練 |

> **セキュキャンの募集時期は公式サイトで要確認**（例年3〜4月頃だが年により変動）。確認したら締切から逆算し直す。